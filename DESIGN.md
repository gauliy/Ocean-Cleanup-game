# Ocean Cleanup Visual Design

## 设计方向

游戏采用明亮、友好、具有教育感的 2D 海洋风格。视觉重点是清澈海水、青绿色强调色、半透明深蓝面板和清楚的游戏数据。界面应具有游戏感，但不能遮挡 Canvas 的主要操作区域。

## 画面尺寸

- Canvas 逻辑尺寸：960 × 540
- 页面最大显示宽度：1080 px
- Canvas 保持 16:9 比例
- 电脑端优先，小窗口下整体等比例缩放

## 色彩

| 用途 | 颜色 |
| --- | --- |
| 深色背景 | `#031D2E` |
| 主面板 | `rgba(4, 43, 64, 0.72)` |
| 主要强调色 | `#16D3C5` |
| 次要强调色 | `#55E5DD` |
| 正文 | `#F8FFFF` |
| 次要文字 | `#B9DCE2` |
| 危险与生命 | `#FF6B6B` |

## 字体

优先使用系统无衬线字体：`Inter`、`Segoe UI`、`sans-serif`。标题粗体，数据使用大号粗体，说明文字保持高对比度。

## 四种状态

### Ready

- 左侧显示标题、介绍、开始按钮和键盘操作。
- 右侧使用任务卡片说明时间、收集垃圾、保护动物和连击奖励。
- 顶部显示游戏名称、最高分和音效按钮。

### Playing

- 顶部 HUD 显示 `Score`、`Time`、`Lives`、`Combo` 和 `Pause`。
- Canvas 使用海洋背景，游戏对象在其上绘制。
- 得分或碰撞反馈使用 Canvas 上方短暂出现的提示条。

### Paused

- 游戏画面保留，但覆盖半透明深色遮罩。
- 中央显示 `Game Paused` 和 `Resume` 按钮。
- 暂停时不显示其他操作按钮。

### Gameover

- 中央结果面板显示等级、最终分数、垃圾数量、保护动物数量和最高连击。
- 环保知识使用带青绿色左边框的区域显示。
- 底部提供 `Play Again` 和 `Back to Home`。

## 游戏对象视觉

- 清理船：约 72 × 72 px，轮廓清楚，方向感明确。
- 垃圾：约 42 至 54 px，不同种类必须容易区分。
- 动物：约 54 至 72 px，颜色自然，不使用危险或敌对视觉。
- 礁石：约 58 至 70 px，灰褐色，与可收集垃圾明显不同。
- 玩家受伤时船闪烁，保护时间结束后恢复正常。

## 动效与反馈

- 收集垃圾：对象消失，显示绿色 `+分数`。
- 五连击：显示 `Combo bonus +20`。
- 碰撞：显示红色警告，清理船短暂闪烁。
- 难度提升：显示 `The current is getting stronger!`。
- 按钮悬停只做轻微上移，不使用强烈缩放。

## 背景素材

`assets/images/ocean-background.png` 为项目生成的 16:9 海洋背景。中央区域保持开阔，珊瑚和岩石集中在边缘及底部，避免影响游戏对象识别。

## 可访问性

- 按钮使用真实 `button` 元素。
- Canvas 提供英文 `aria-label`。
- 颜色反馈必须同时配合文字和数值变化。
- 键盘操作不能依赖大小写。
- 正文与背景保持清楚对比。
